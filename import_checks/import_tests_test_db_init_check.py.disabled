"""Disabled import-only check for tests.test_db_init

The original checked that importing 'tests.test_db_init' succeeded during
development. It's disabled during automated test collection because it
pre-imports test modules and causes pytest import-file mismatch errors.
"""

# File intentionally disabled to avoid pre-importing tests during pytest
